logging:
  level:
    root: warn
    me.jung.demokakaoapi: debug
    org.hibernate.type.descriptor.sql: trace
spring:
  profiles: local
  datasource:
    url: jdbc:mariadb://demokakao.cavst1k23eg8.ap-northeast-2.rds.amazonaws.com:3306/demokakao
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        jdbc.lob.non_contextual_creation: true
    showSql: true
